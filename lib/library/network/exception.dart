class UnsupportedException implements Exception {
  final String message;
  UnsupportedException(this.message);
}

class NetworkExceptioon implements Exception {
  final String message;
  NetworkExceptioon(this.message);
}
